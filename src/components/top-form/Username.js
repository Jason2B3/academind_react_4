import styled from "styled-components"; // REQUIRED for SC

const Username = (props) => {
  return (
    <div className="container">
      
      
      <label>Username</label>
      <input
        type="text"
        // value={enteredValue}
        // onChange={goalInputChangeHandler}
      />
    </div>
  );
};
export default Username;

//% SC-CSS incoming
