const Age = (props) => {
  return(<div></div>)
  /*
  return (
    <div className={`${validInput ? "" : styles.invalid}`}>
      
      <label>Age (Years)</label>
      <input
        type="number"
        step="1"
        // value={enteredValue}
        // onChange={goalInputChangeHandler}
      />
    </div>
  );
  */
};
export default Age;

//% SC-CSS incoming
